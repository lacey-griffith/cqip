import { createClient } from '@supabase/supabase-js';

const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL;
con{
  "name": "cqip",
  "version": "0.1.0",
  "template": "next",
  "base": "radix"serv  "version": "0.th  "template": "next"ng  "base": "radix",
 ir  "style": "css",);  "css": {
    " s    "varimi  },
  "components":ab  "Ur}
